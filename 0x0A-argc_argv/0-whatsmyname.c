#include "main.h"
#include <stdio.h>
/**
 *main - prints its program.
 *@argc: the nuber program commandline.
 *@argv: The array of size of argc.
 *Return: 0, always success.
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
